PORT=3001
JWT_SECRET=change-this-to-a-long-random-secret
DB_PATH=./data/ulysses.db

# Email (SMTP) — leave blank to use console-log mode in dev
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=noreply@example.com
SMTP_PASS=your-smtp-password
SMTP_FROM="Ulysses Note <noreply@example.com>"

# SMS — leave blank to use console-log mode in dev
# SMS_ACCESS_KEY_ID=your-aliyun-key
# SMS_ACCESS_KEY_SECRET=your-aliyun-secret
# SMS_SIGN_NAME=YourSignName
# SMS_TEMPLATE_CODE=SMS_xxxxxxxxx
